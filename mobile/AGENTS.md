# Native Instructions

These instructions apply to `mobile/`.

- This is the `native` React Native host app.
- Keep native host work in `mobile/src/`, `mobile/index.ts`, and native project files.
- The current native app is a skeleton: preserve the shell and Pear platform setup, but do not reintroduce old document/editor guts without rebuilding them on `pear-sdk/spaces`.
